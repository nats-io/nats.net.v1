authorization {
  BASIC = {
    publish: {
      allow: [
                "sub.>"
                ]
    }
    subscribe: {
      allow: [
                "sub.>"
                ]
    }
  }
  users = [
    {user: uuu, password: ppp, permissions: $BASIC}
  ]
}
